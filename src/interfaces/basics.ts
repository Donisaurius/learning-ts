(() => {
  interface Hero {
    name: string;
    age: number;
  }

  //Se comporta similarmente como los types, solo sin usar el =
  //LOS TIPOS NO PUEDEN EXTENDERSE
  //LAS CLASES SI SE PUEDEN EXTENDER. Se pueden usar para las peticiones http
})();
